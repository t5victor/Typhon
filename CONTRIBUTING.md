# Contributing

Run `bazel test //...` before submitting a change. Commands must express a business decision and
events an irreversible business fact. Add aggregate, adapter, and replay tests whenever a stream or
event contract changes.
